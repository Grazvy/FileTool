## Rules
- Don't make changes beyond what is requested
- Work only with the default environment "CursorDefault", never create or switch the environment
- It is prohibitted to use any files outside of the project directory 
- Do not execute anything that delegates cleanup such as closing a port or a file
- Stick to the architecture description provided in project_overview.md